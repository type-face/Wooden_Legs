%p
  =form_tag search_results_path, method: "get" do
    = submit_tag("Search")
    = text_field_tag(:search_terms) 
    in
    = collection_select(:search_category, :ids, @categories, :id, :name, prompt: "any") 
    category

